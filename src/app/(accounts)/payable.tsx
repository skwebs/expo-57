import { Text, View } from "react-native";

const PayableScreen = () => {
  return (
    <View className="flex-1 pt-safe">
      <Text>PayableScreen</Text>
    </View>
  );
};

export default PayableScreen;
